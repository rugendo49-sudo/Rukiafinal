{
  "name": "omoka",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --port 3000 --strictPort\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
